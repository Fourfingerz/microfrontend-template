name: deploy-dashboard-azure

on:
  push:
    branches:
      - az
    paths:
      - 'packages/dashboard/**'

defaults:
  run:
    working-directory: packages/dashboard

env:
  AWS_DEFAULT_REGION: 'us-east-1'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: azure/login@v1
        with:
            creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: upload to blob storage
        uses: azure/cli@v1
        with:
          inlinescript: |
              az storage blob upload-batch --account-name ${{ secrets.storage_account_name }} --auth-mode key -d '$web/container/dashboard' -s $GITHUB_WORKSPACE/packages/dashboard/dist
              az storage blob upload-batch --account-name ${{ secrets.storage_account_name }} --auth-mode key -d '$web/dashboard' -s $GITHUB_WORKSPACE/packages/dashboard/dist
      # - name: Purge CDN endpoint
      #   uses: azure/CLI@v1
      #   with:
      #     inlineScript: |
      #       az cdn endpoint purge --content-paths  "/*" --profile-name "CDN_PROFILE_NAME" --name "CDN_ENDPOINT" --resource-group "RESOURCE_GROUP"

      # Azure logout
      - name: logout
        run: |
              az logout
        if: always()